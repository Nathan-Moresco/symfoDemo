{% extends 'base.html.twig' %}

{% block content %}
    <div class="mb-4 flex h-1/3 w-1/2 justify-center rounded-2xl bg-primary p-6 shadow-lg">

        <div class="flex flex-col items-center justify-center gap-2">
            <span class="mb-2 text-xl font-semibold text-light">{{ "Bienvenue sur votre espace compte" }}</span>

            <div class="flex items-center rounded-lg bg-secondary px-4 py-2 font-semibold text-primary">
                <span>{{ app.user.email }}</span>
            </div>

            {{ dump(app.user)}}

        </div>
{% endblock %}