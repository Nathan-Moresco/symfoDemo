{% extends 'base.html.twig' %}

{% block content %}
    <div class="bg-primary rounded-2xl flex justify-center p-6 w-1/2 h-1/3 shadow-lg mb-4">
        <div class="flex flex-col items-center justify-center gap-2">
            <span class="text-xl text-light mb-2">{{ "Connexion à votre compte" }}</span>

            {{ form(login_form) }}


            <a class="common-button bg-secondary h-8 mt-2">{{ "Connexion"|trans }}&nbsp;<i class="fa fa-arrow-right-to-bracket"></i></a>
            <a class="text-sm text-cyan-600 mt-2">{{ "Je n'ai pas de compte"|trans }}</a>
        </div>
        {# <div class="flex flex-col w-1/2 items-start justify-center gap-2">
            <span class="text-xl text-light mb-2">{{ "Vous n'avez pas de compte ?" }}</span>
            
            {{ form(register_form) }}

            <a class="common-button bg-secondary h-8 mt-2">{{ "Connexion"|trans }}&nbsp;<i class="fa fa-arrow-right-to-bracket"></i></a>
        </div> #}
    </div>
{% endblock %}