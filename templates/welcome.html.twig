{% extends 'base.html.twig' %}

{% block content %}
    <div class="bg-primary rounded-2xl flex items-center justify-center p-6 w-1/2 h-1/3 shadow-lg mb-4">
        <span class="text-4xl font-bold text-light">{{ "Bienvenue"|trans }}</h1>
    </div>

    <div class="w-full grid grid-cols-3 gap-6 mb-4">
        {% for i in 1..3 %}
            <div class="relative bg-primary w-full h-32 rounded-xl p-4"></div>
        {% endfor %}
    </div>
    
    <div class="w-full grid grid-cols-4 gap-6 mb-4">
        {% for i in 1..8 %}
            <div class="relative bg-primary w-full h-32 rounded-xl p-4"></div>
        {% endfor %}
    </div>
{% endblock %}